[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "local81"
version = "0.1.0"
description = "Local-81 Python control plane"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: System :: Systems Administration",
]
# Stdlib-first: PyYAML is the ONLY third-party runtime dependency. For
# air-gapped / pip-less builds it is vendored under vendor/yaml and bundled into
# a single zipapp by `make zipapp` (see packaging/zipapp/README.md).
dependencies = [
  "PyYAML>=6.0",
]

[project.scripts]
local81 = "local81.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff>=0.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 160
target-version = "py312"

[tool.ruff.lint]
select = ["E9", "F"]
